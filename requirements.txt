# ── AI Translation Provenance System — Python Dependencies ────────────────────

# ── Core Framework ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9

# ── Haystack (semantic search) ────────────────────────────────────────────────
haystack-ai>=2.3.0
sentence-transformers>=3.0.0

# ── Translation Backends (install what you need) ──────────────────────────────
# Anthropic Claude
anthropic>=0.30.0

# DeepL (optional)
# deepl>=1.18.0

# Google Cloud Translation (optional)
# google-cloud-translate>=3.15.0

# Phase 16 — OpenAI, Gemini, Microsoft Translator, LMStudio, vLLM, and
# Ollama/Tower+ are all implemented as raw REST calls over httpx below
# (app/core/llm_clients.py, app/core/translation_backends.py,
# app/core/scoring/*_scorer.py) rather than provider SDKs — no new
# packages needed for any of them.

# ── HTTP Client (for tests and async calls) ───────────────────────────────────
httpx>=0.27.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
anyio>=4.0.0

# ── Database (PostgreSQL, async) ───────────────────────────────────────────────
asyncpg>=0.29.0                 # PostgreSQL async driver
sqlalchemy[asyncio]>=2.0.0      # ORM layer
alembic>=1.13.0                 # Schema migrations
pgvector>=0.2.0                 # pgvector SQLAlchemy type — Phase 13's style-guide/glossary/
                                 # exemplar embedding columns (requires the `vector` Postgres
                                 # extension, already available in pgvector/pgvector:pg16)

# ── Non-cooperative page review (Phase 8) ─────────────────────────────────────
playwright>=1.47.0              # Headless-browser render for arbitrary/JS-rendered pages
                                 # Run `playwright install chromium` once after pip install

# ── Site i18n/compliance audit toolkit (Phase 11/12) ────────────────────────────
langdetect>=1.0.9                # Language detection for the mixed-locale and privacy-policy checks
reportlab>=4.2.0                 # Branded PDF report generation for audit findings

# ── Automatic quality metrics (Phase 15) ────────────────────────────────────────
nltk>=3.9                        # METEOR (app/core/scoring/automatic/meteor.py) — pure CPU, no GPU.
                                  # One-time corpus download on first use: nltk.download('wordnet')

# COMET-Kiwi (optional — NOT installed by default; see
# app/core/scoring/automatic/comet_kiwi.py's module docstring). Pulls in torch +
# transformers + pytorch-lightning (multiple GB); the wmt22-cometkiwi-da checkpoint
# is CC-BY-NC-SA-4.0 and requires a free Hugging Face login + one-click license
# acceptance at https://huggingface.co/Unbabel/wmt22-cometkiwi-da before
# download_model() will succeed. See docs/quality-evaluation-research.md §3/§8.1.
# unbabel-comet>=2.2.0

# ── Vector Stores (optional alternatives to in-memory) ────────────────────────
# qdrant-client>=1.9.0          # Qdrant
# elasticsearch>=8.0.0          # Elasticsearch

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
